language: php

php:
  - '8.1'
  - '8.2'
  - '8.3'
  - '8.4'

cache:
  directories:
    - $HOME/.composer/cache

install:
  - composer install --prefer-dist --no-progress

script:
  - vendor/bin/phpunit
  - vendor/bin/phpstan analyse --no-progress
  - vendor/bin/phpcs
